<div title="{dateFormat $date DlongD2Mlong} - {Label event.type.birthday} {htmlencode $title} ({$age} {Label calendar.yearsold})" id="birthday-{$id}" class="event typeBirthday quickInfoBirthday">
	<div class="head">
		{if $calendarMode == 'day'}
			{htmlencode $fullname}
		{else}
			{htmlencode $fullname|truncate:16}
		{/if}
	</div>
</div>